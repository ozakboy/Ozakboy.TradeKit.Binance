# ── 建置產物 ──
**/bin/
**/obj/
/artifacts/
*.user
*.suo

# ── IDE ──
.vs/
.idea/
*.DotSettings.user

# ── 測試與涵蓋率 ──
/TestResults/
**/TestResults/
*.coverage
coverage*.json
coverage*.xml
*.trx

# ── 套件輸出 ──
*.nupkg
*.snupkg

# ── 作業系統 ──
Thumbs.db
desktop.ini
.DS_Store

# ── 機密(絕不進版控)──
.claude/secrets.local.json
**/secrets.local.json
.env
.env.*
!.env.example
*.pem
*.pfx
*.key

# 暫時性:Abstractions 0.4.0 發佈前,還原目的地由 NuGet.config 指到這裡,連同該檔一起刪除。
# TEMPORARY: restore target while Abstractions 0.4.0 is unpublished; remove along with NuGet.config.
/.nuget-packages/
